🧩 Proyecto Integrador — “Mi Perfil Web” (Versión Multimedia)

Objetivo:
Actualizar tu proyecto proyecto_perfil agregando imágenes, audio y video.

✅ Tareas:

Crea carpetas dentro del proyecto:

imagenes/
audio/
video/


Añade:

Una imagen personal o avatar (foto_perfil.jpg)
Un audio (puede ser libre o grabación propia)
Un video corto de presentación o fondo
Integra estos recursos en index.html.
Usa etiquetas <img>, <audio> y <video> correctamente.
Verifica que las rutas funcionen localmente.

🧰 Mini Tarea GIT — Nivel 3: Historial, Diferencias y Reversión

1️⃣ Crea una rama de mejora multimedia
git switch -c multimedia

2️⃣ Añade tus archivos multimedia y realiza un commit inicial
git add .
git commit -m "Añadidos archivos multimedia (imagen, audio, video)"

3️⃣ Realiza una modificación (por ejemplo, cambia el texto de un encabezado) y revisa las diferencias
git diff

4️⃣ Si quieres revertir los cambios del archivo antes de confirmar:
git restore index.html

5️⃣ Si ya confirmaste un commit y deseas deshacerlo:
git log --oneline
git revert <id_del_commit>

6️⃣ Fusiona la rama con los cambios confirmados
git switch main
git merge multimedia

7️⃣ Sube tu nueva versión a GitHub
git push origin main


//////////////////////////////////
1. Crear la Rama y Cambiar a Ella
git checkout -b multimedia

2. Realizar y Confirmar los Cambios
    Modifica el archivo
    Añade (Staging) los cambios
        git add index.html
    Realiza el commit (Confirmar)
        git commit -m "Se agregaron ajustes y elementos multimedia en index.html"

3. Volver a main y Fusionar
    Volver a la rama principal:
        git checkout main
    Fusionar la rama multimedia en main
        git merge multimedia
    Opcional: Eliminar la rama multimedia
        git branch -d multimedia
    
Para subir tus cambios a GitHub
git push


